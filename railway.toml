[deploy]
startCommand = "gunicorn app:app --bind 0.0.0.0:$PORT --timeout 180 --workers 1 --threads 4 --worker-class sync --access-logfile - --error-logfile -"
healthcheckPath = "/health"
healthcheckTimeout = 300
